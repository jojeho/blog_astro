---
import Layout from "../layouts/Layout.astro"
export const prerender = false;
---
<Layout title="search">
    <div>Page</div>
    {[1, 2, 3].map((page) => (
        <a 
            href="#" 
            hx-get={`/partials/list?page=${page}`}
            hx-target="#contents"
            hx-params="*"
          
        >{page}</a>
    ))}

<div id="contents" >
  aaaa
</div>




  </Layout>

  <style>
    .ab {
      color:red
    }
  </style>