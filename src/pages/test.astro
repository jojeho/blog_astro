---
import Layout from "../layouts/Layout.astro"

---
<Layout title="search">

    <div>Page</div>
    {[1, 2, 3].map((page) => (
        <a 
            href="#" 
            hx-get={`/partials/list?page=${page}`}
            hx-target="#contents"
            hx-params="*"
            hx-boost="true"
          
        >{page}</a>
    ))}

<div id="contents" >
  aaaa
</div>

<style>

</style>

